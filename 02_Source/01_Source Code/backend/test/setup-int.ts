// Thêm các thiết lập toàn cục cho Integration Test ở đây nếu cần
// Ví dụ: Thiết lập timeout dài hơn cho các thao tác database
jest.setTimeout(30000);

// Có thể thêm một số global hooks ở đây
/*
beforeAll(async () => {
  // Global setup
});

afterAll(async () => {
  // Global teardown
});
*/
